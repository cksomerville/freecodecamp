This project constists of:
    - PostgreSQL Database
    - element.sh
    - git repository

The database holds information on different elements.

element.sh is a bash script that will fetch any element (when given an argument) from the database and serve the data in the terminal.